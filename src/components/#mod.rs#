//pub mod base;

//pub mod button;

pub mod container;

pub mod attributes;

Maybe we just want a component struct that has the base component like position

    And an enum of Component type that can be button, text area and other stuff 
